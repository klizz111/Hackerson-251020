## Usage 

1. > cd ./backend
2. > pipenv shell # 进入虚拟环境
2. > pip install -r requirements.txt
3. > python ./main.py

如需使用SSL，在运行前先运行`./install.sh`脚本，成功后再运行`main.py`，并在[https://localhost:443](https://localhost:443)访问。